# 数据保护与验证

考勤数据只保存在应用私有目录；系统云备份和设备迁移规则均排除
`attendance_data.xml`。手动 JSON 备份仍为明文，包含学生姓名、学号、原因、
历史及草稿。请仅保存到可信位置。暂未提供密码加密备份。

导入名单限制为 1 MiB、每班 5,000 人，完整备份限制为 8 MiB，JSON 嵌套最多
32 层。备份中的班级、学生、记录 ID、状态及关联在写入前校验，历史学生快照
允许引用已从名单移除的学生。重复常用原因规范化为唯一列表。

CSV 按完整记录解析，姓名/学号禁止换行和控制字符。面向表格软件导出时，
公式前缀 `= + - @` 前添加单引号并引用字段。这会改变这些字段的导出文本；
原值通过 JSON 备份保留。不同表格软件对单引号和再次保存 CSV 的处理不同，
不能把该措施视为所有软件中的绝对保证。

写入由单例仓库的串行后台线程完成，完整快照通过一次 `commit()` 更新。
只有确认成功的快照才成为恢复基准，另保留上一份回滚快照；结束点名时，
历史与草稿清理属于同次提交。加载校验失败或写入失败会阻止继续修改，并提供
原始数据导出和恢复入口。原始恢复导出用于人工排查，不等同于标准备份文件。

## 验证

`./gradlew testDebugUnitTest lintDebug` 包含 CSV、字节限制、重复 ID、无效状态、
历史快照、失败写入与恢复的回归用例。`Manual code checks` 工作流可手动运行。
构建和校验均不会因推送自动触发。

Actions 使用经上游仓库解析的完整提交 SHA。Gradle 8.9 分发 ZIP 与现有 Wrapper
JAR 的 SHA-256 已对照 [Gradle 官方校验表](https://gradle.org/release-checksums/) 核对。
尚未生成完整传递依赖的 verification-metadata.xml：需要在可联网的可信构建环境
下载依赖、生成清单并人工核验，不能填入未经核验的摘要。

当前开发环境无法下载 Gradle，新增 JVM 回归用例和 Android lint 尚未运行；
未完成实机压力测试、表格软件兼容验证或全部依赖漏洞扫描。
